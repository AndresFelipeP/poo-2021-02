package poo.Desafio2.dominio;

import java.util.ArrayList;

public class CoordinadorVentas extends Directo{
    public long sumaDeVentas;

    private ArrayList<Empleado> empleados=new ArrayList<>();

    public CoordinadorVentas(String nombre, long salario) {
        super(nombre, salario);
        this.sumaDeVentas= sumaDeVentas;
    }

    public long CalcularComision() {
        public ArrayList<Empleado> empleados = new ArrayList<>();
        long saldo = 0;
        saldo = salario..............;
        salario = (long) (sumaDeVentas * 0.98);
    }
i
    @Override
    public long calcularSalario() {

        return this.salario+CalcularComision()-(calcularPension()+calcularSalud());


}
